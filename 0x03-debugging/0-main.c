#include "main.h"
void positive_or_negative(int i);
/**
 * main - test the functions that print if an integer is positive or negative
 * Return: 0
 */

int main(void)
{
	int i;

	i =0;
	positive_or_negative(i);

	return (0);
}
